/* eslint-disable simple-import-sort/imports */
import Button from '@/components/ui/Button/Button';
import React from 'react';

const Discover = () => {
  return (
    <div className="border">
      <Button>All Discover</Button>
    </div>
  );
};

export default Discover;
